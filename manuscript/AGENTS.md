# `manuscript/` — Agent Guide

The paper about the coordination testbed.

**Contents.** IMRaD sections (`00_abstract`..`99_references`) + `config.yaml` (single source of truth) + `references.bib`. Renders to a standard PDF via the pipeline.

**Contract.** Numbers are token-bound to generated artifacts via the evidence registry; keep claims sourced.

See the project [`../AGENTS.md`](../AGENTS.md).
